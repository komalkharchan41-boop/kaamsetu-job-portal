<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>KaamSetu - Smart Job Platform</title>
  <style>
    * {margin:0; padding:0; box-sizing:border-box; font-family: 'Segoe UI', Arial, sans-serif;}

    body {
      background: url('https://images.unsplash.com/photo-1504384308090-c894fdcc538d?auto=format&fit=crop&w=1500&q=80') 
                  no-repeat center center/cover;
      color: #fff;
      min-height: 100vh;
    }

    header {
      background: rgba(0, 123, 255, 0.85);
      color: white;
      padding: 20px;
      text-align: center;
      font-size: 26px;
      font-weight: bold;
      backdrop-filter: blur(5px);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    #profileIcon {
      display: none;
      cursor: pointer;
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: url('https://cdn-icons-png.flaticon.com/512/149/149071.png') center/cover no-repeat;
      border: 2px solid white;
    }

    nav {
      display: flex;
      justify-content: center;
      background: rgba(0, 86, 179, 0.9);
      flex-wrap: wrap;
    }

    nav button {
      background: none;
      border: none;
      color: white;
      padding: 14px 22px;
      font-size: 16px;
      cursor: pointer;
      transition: 0.3s;
    }

    nav button:hover {
      background: rgba(255, 255, 255, 0.2);
      border-radius: 6px;
    }

    .section {
      display: none;
      padding: 30px;
      animation: fadeIn 0.6s ease-in-out;
    }

    .active {
      display: block;
    }

    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }

    .card {
      background: rgba(255, 255, 255, 0.9);
      color: #333;
      padding: 25px;
      border-radius: 14px;
      box-shadow: 0px 6px 15px rgba(0,0,0,0.2);
      max-width: 900px;
      margin: 20px auto;
      backdrop-filter: blur(6px);
    }

    h2 {
      color: #007bff;
      margin-bottom: 12px;
    }

    input, textarea, select {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 15px;
    }

    button.primary {
      background: linear-gradient(135deg, #007bff, #0056b3);
      color: white;
      padding: 12px 22px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      margin-top: 12px;
      font-weight: bold;
      transition: 0.3s;
    }

    button.primary:hover {
      background: linear-gradient(135deg, #0056b3, #003d80);
    }

    .error {
      display: none;
      color: red;
      margin-top: 10px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <header>
    <h1>KaamSetu - Smart Job Platform</h1>
    <div id="profileIcon" onclick="showSection('profile')"></div>
  </header>

  <nav>
    <button onclick="showSection('home')">Home</button>
    <button onclick="showSection('login')">Login</button>
    <button onclick="showSection('jobSeekers')">Job Seekers</button>
  </nav>

  <footer style="background: rgba(0,0,0,0.7); color: #fff; text-align: center; padding: 30px 15px; font-size: 14px; margin-top: 40px;">
    <div style="max-width: 900px; margin: auto;">
      <p>© 2025 KaamSetu | Designed & Developed by <strong>Your Name / Team</strong></p>
    </div>
  </footer>

  <!-- Home Section -->
  <section id="home" class="section active">
    <div class="card">
      <h2>Welcome to KaamSetu</h2>
      <p>Connecting local job seekers with employers in your community.</p>
    </div>
  </section>

  <!-- Login Section -->
  <section id="login" class="section">
    <div class="card">
      <h2>Login</h2>
      <input type="text" id="aadhar" maxlength="12" placeholder="Enter Aadhaar Number">
      <button class="primary" onclick="sendOTP()">Send OTP</button>
      <div id="otpBox" style="display:none;">
        <input type="text" id="otp" maxlength="6" placeholder="Enter OTP">
        <button class="primary" onclick="verifyLogin()">Login</button>
      </div>
      <p id="error" class="error">Invalid details. Try again.</p>
    </div>
  </section>

  <!-- Profile Section -->
  <section id="profile" class="section">
    <div class="card">
      <h2>Your Profile</h2>
      <input type="text" id="fullName" placeholder="Full Name">
      <input type="text" id="skills" placeholder="Skills">
      <input type="number" id="age" placeholder="Age">
      <input type="text" id="education" placeholder="Education">
      <input type="text" id="location" placeholder="Location">
      <input type="text" id="contact" placeholder="Contact Number">
      <input type="text" id="jobType" placeholder="Job Type">
      <input type="text" id="experience" placeholder="Experience">
      <input type="email" id="gmail" placeholder="Gmail">
      <textarea id="resume" placeholder="Resume / Qualification"></textarea>
      <button class="primary" onclick="saveProfile()">Save Profile</button>
    </div>
  </section>

  <!-- Job Seekers Section -->
  <section id="jobSeekers" class="section">
    <div class="card">
      <h2>Available Job Seekers</h2>
      <ul id="jobSeekerList"></ul>
    </div>
  </section>

  <script>
    function showSection(id) {
      document.querySelectorAll(".section").forEach(s => s.classList.remove("active"));
      document.getElementById(id).classList.add("active");
    }

    let generatedOTP = "";

    function sendOTP() {
      let aadhar = document.getElementById("aadhar").value;
      if (aadhar.length !== 12) {
        alert("Enter valid 12-digit Aadhaar number.");
        return;
      }
      generatedOTP = Math.floor(100000 + Math.random() * 900000).toString();
      alert("Your OTP: " + generatedOTP);
      document.getElementById("otpBox").style.display = "block";
    }

    function verifyLogin() {
      let otp = document.getElementById("otp").value;
      if (otp === generatedOTP) {
        alert("Login Successful!");
        document.getElementById("profileIcon").style.display = "block";
        showSection('profile');
      } else {
        document.getElementById("error").style.display = "block";
      }
    }

    function saveProfile() {
      const name = document.getElementById("fullName").value;
      const skills = document.getElementById("skills").value;
      const age = document.getElementById("age").value;
      const edu = document.getElementById("education").value;
      const loc = document.getElementById("location").value;
      const contact = document.getElementById("contact").value;
      const jobType = document.getElementById("jobType").value;
      const exp = document.getElementById("experience").value;
      const gmail = document.getElementById("gmail").value;
      const resume = document.getElementById("resume").value;

      if (name && skills && age && edu && loc && contact && jobType && exp && gmail) {
        const li = document.createElement("li");
        li.innerHTML = `<strong>${name}</strong> | ${age} | ${edu} | ${loc} | ${contact} | ${jobType} | ${exp} | ${gmail} | ${resume}`;
        document.getElementById("jobSeekerList").appendChild(li);
        alert("Profile saved and added to Job Seekers list!");
      } else {
        alert("Please fill all fields before saving.");
      }
    }
  </script>
</body>
</html>
